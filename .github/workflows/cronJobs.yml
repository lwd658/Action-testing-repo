on:
  schedule:
    - cron: '32 12 * * *'
    - cron: '34 12 * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: first
        if: github.event.schedule = '40 12 * * *'
        run: echo "First"
      - name: second
        if: github.event.schedule = '50 12 * * *'
        run: echo "Second"
