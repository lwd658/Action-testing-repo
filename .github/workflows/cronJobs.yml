on:
  schedule:
    - cron: '30 17 * * *'
    - cron: '32 17 * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: first
        if: github.event.schedule == '30 17 * * *'
        run: echo "First"
      - name: second
        if: github.event.schedule == '32 17 * * *'
        run: echo "Second"
