on:
  schedule:
    - cron: '50 16 * * *'
    - cron: '55 16 * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: first
        if: github.event.schedule = '50 16 * * *'
        run: echo "First"
      - name: second
        if: github.event.schedule = '55 16 * * *'
        run: echo "Second"
