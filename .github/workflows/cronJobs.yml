on:
  schedule:
    - cron: '00 13 * * *'
    - cron: '55 17 * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: first
        if: github.event.schedule == '00 13 * * *'
        run: curl -u ${{ secrets.DB_14_RUN }}
      - name: second
        if: github.event.schedule == '55 17 * * *'
        run: echo "second"
